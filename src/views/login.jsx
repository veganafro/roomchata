const React = require('react');
const PropTypes = require('prop-types');
const Base = require('./base');

class Login extends React.Component {
    render() {
        return (
            <Base title='Login'>
            <h3 className='login_header'>roomchata</h3>
            <form method='POST'>
            </form>
            </Base>
        );
    }
}
